fastapi
uvicorn[standard]
pydantic>=2.6,<3
transformers>=4.40,<5
peft>=0.11
accelerate>=1.0,<2

# 只在 Linux/macOS 安裝（Windows 容易失敗）
bitsandbytes>=0.41 ; platform_system != "Windows"
faiss-cpu>=1.7 ; platform_system != "Windows"

chromadb>=1.0.0
sentence-transformers>=3.0
spacy<3.9
scikit-learn>=1.3
numpy>=1.26
pandas>=2.1

# Windows 用有 GUI 的 OpenCV；CI/Linux 用 headless 版
opencv-python ; platform_system == "Windows"
opencv-python-headless ; platform_system != "Windows"

pydub
python-multipart
