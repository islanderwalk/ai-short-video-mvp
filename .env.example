# —— Provider 選擇 ——
# local  = 本機 Hugging Face 模型（會自動下載並快取）
# openai = 使用 OpenAI API （最適合你現在測試）
CAPTION_PROVIDER=openai

# —— local 模式設定（Hugging Face）——
# 可以填模型名稱（會自動下載）或本地路徑（離線）
CAPTION_BASE_MODEL=sshleifer/tiny-gpt2
# 量化（可空白 / int8 / int4）
CAPTION_QUANT=
# HUGGINGFACE_HUB_TOKEN=hf_xxx   # 私有模型才需要

# —— openai 模式設定 ——
OPENAI_API_KEY=sk-xxxx
OPENAI_MODEL=gpt-4o-mini   # 也可：gpt-4o, gpt-4.1, gpt-3.5-turbo 等

# （進階）如果你用自建的兼容端點（如 vLLM / Azure 等），可覆蓋：
# OPENAI_BASE_URL=https://your-proxy.example.com/v1


